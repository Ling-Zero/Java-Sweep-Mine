# 扫雷游戏（三关扫雷）—— 代码深度解析

> 本文对每个类的每个方法做逐行/逐算法解析，并解释关键设计决策。

---

## 〇、整体数据流

```
鼠标(MouseEvent)
   │ mousePressed → 坐标换算 (r,c) → onLeftClick / onRightClick
   ▼
MineFieldPanel(视图/控制器)
   │ model.open() / model.toggleFlag()
   ▼
MineField(模型) ──事件──> MineField.Listener ──转发──> MineFieldPanel.Listener ──> AppWindow
   （布雷/翻开/判定）        onGameStart/onGameOver/onMineCountChanged     更新标签·计时·胜负流程
```

---

## 一、core.MineField.java —— 逐个方法

### 状态字段（二维数组的分工）
```java
boolean[][] mines;      // 该格是否为地雷
boolean[][] revealed;   // 是否已翻开
boolean[][] flagged;    // 是否已插旗
int[][]     around;     // 周围 8 格的地雷数（“数字”）
boolean started;        // 是否已布雷（首击后才布雷）
boolean finished;       // 是否已结束（胜/负）
int openedSafe;         // 已翻开的安全格数（用于判胜）
int flagCount;          // 已插旗数（剩余雷数 = mineCount - flagCount）
```
- `mines/revealed/flagged` 三者相互独立：一个格可以同时是雷+已翻开（失败后揭开），也可以是标记但非雷（标错）。
- `around` 是计算出来的“数字”，只有翻开后才会被界面画出来。

### Listener 接口（模型不碰 UI）
模型通过三个回调让外界知道状态变化，且**只在这些时机触发**：
- `onGameStart()`：仅在 `open()` 里**首次点击、布雷完成后**触发（用于 AppWindow 启动计时）。
- `onMineCountChanged(remaining)`：在 `toggleFlag()` 每次切换、以及 `finish(true)` 自动插旗后触发。
- `onGameOver(won)`：在 `finish()` 里稳定触发一次。

### open(r,c) —— 决策树
```java
if (finished)            return IGNORED;     // 已结束，忽略后续点击
if (revealed[r][c])      return ALREADY_OPEN;// 已翻开；交给视图做 chord，模型不动
if (flagged[r][c])       return FLAGGED;     // 已标记；左键不翻开（需先取消标记）
if (!started) {                              // 第一次点击
    started = true;
    placeMines(r, c);                        // 布雷，且保证 (r,c) 不是雷
    listener.onGameStart();                  // 通知启动计时
}
if (mines[r][c]) { finish(false); return LOST; }   // 踩雷 → 判负
floodReveal(r, c);                                 // 翻开（含空白扩散）
if (openedSafe == rows*cols - mineCount) { finish(true); return WON; } // 全安全格翻开 → 判胜
return REVEALED;                                   // 普通翻开
```
- 用 `OpenResult` 枚举让**视图**据此决定：`REVEALED` 才播音效，`LOST/WON` 时停止 chrd 并弹窗，`ALREADY_OPEN` 走 chord。
- 关键点：**先布雷再判断踩雷**，所以第一击必定安全（经典扫雷的“首击安全”）。

### placeMines(safeR, safeC) —— 布雷 + 计算数字
```java
// 1) 随机布雷（拒绝采样）
while (placed < mineCount) {
    int r = rnd.nextInt(rows), c = rnd.nextInt(cols);
    if ((r==safeR && c==safeC) || mines[r][c]) continue; // 跳过首击格&已布雷格
    mines[r][c] = true; placed++;
}
// 2) 计算每个格子的数字
for (每个地雷 (r,c))
    for (dr in -1..1) for (dc in -1..1)
        if (inRange(r+dr, c+dc)) around[r+dr][c+dc]++;   // 给 8 邻格 +1
```
- 若两个雷相邻，中间格会被两枚雷各自 +1 → 数字正确。
- `rnd.nextInt(bound)` 是**拒绝采样**：重复取直到放够，不会越界、不会漏放。

### floodReveal(r,c) —— 空白扩散（BFS）
```java
ArrayDeque<int[]> queue = new ArrayDeque<>();
queue.add({r,c});
while (!queue.isEmpty()) {
    int[] p = queue.poll();
    int pr=p[0], pc=p[1];
    if (!inRange(pr,pc) || revealed[pr][pc] || flagged[pr][pc] || mines[pr][pc])
        continue;                          // 越界/已翻开/已标记/是雷 → 跳过
    revealed[pr][pc] = true; openedSafe++;
    if (around[pr][pc] == 0)               // 是空白格
        for (8 邻) queue.add(邻居);        // 继续扩散
}
```
- 效果：点一个空白，会把整片空白及其**边界一圈的数字格**都翻开（数字格只翻开自身，不继续扩散）。
- 队列**不去重**：一个格可能被多个空白邻格重复入队，但 `revealed` 守卫保证只处理一次，幂等、不重复计数 `openedSafe`。
- 用 `ArrayDeque<int[]> ` 比递归栈安全（不会爆栈，棋盘再大也行），且比封装 `Point` 省对象。

### toggleFlag(r,c) —— 标记
```java
if (finished || revealed[r][c]) return;    // 已结束/已翻开不能标记
flagged[r][c] = !flagged[r][c];
flagCount += flagged[r][c] ? 1 : -1;
listener.onMineCountChanged(getRemainingMines());
```

### finish(won) —— 收尾
```java
finished = true;
if (won)  { for 每雷 if (!flagged) { flagged=true; flagCount++; }
            listener.onMineCountChanged(0); }       // 剩余雷数清零
else      { for 每雷 revealed = true; }             // 揭开所有雷
listener.onGameOver(won);
```

---

## 二、ui.MineFieldPanel.java —— 逐个方法

### 构造：模型接线 + 初始尺寸
```java
model = new MineField(cfg, new Listener(){ 三个方法原样转发给外部 listener });
numberFont = getFont().deriveFont(BOLD, 18);
mineBomb = GameIcons.bomb(24).getImage();                 // 预生成炸弹图
setPreferredSize(cols*BASE_CELL + 2*PAD, rows*BASE_CELL + 2*PAD); // 初始窗口尺寸
addMouseListener(mousePressed -> { 坐标换算 → onLeftClick/onRightClick });
listener.onMineCountChanged(model.getRemainingMines()); // 初始显示剩余雷数
```
- 模型事件**原样转发**：面板本身不处理事件，只当“中转站”，最终由 AppWindow 的监听器做业务。
- `mineBomb` 只生成一次，绘制时按格子缩放，避免每个雷格重复创建图片。

### 自适应格子（paintComponent 内的数学）
```java
int availW = Math.max(0, getWidth() - 2*PAD);
int availH = Math.max(0, getHeight() - 2*PAD);
int target = (int) Math.min((double)availW/cols, (double)availH/rows); // 取较约束的一维
cellSize = Math.max(MIN_CELL, Math.min(MAX_CELL, target)); // 14~48
boardW = cellSize*cols; boardH = cellSize*rows;
originX = (getWidth() - boardW)/2;   // 居中
originY = (getHeight() - boardH)/2;
```
- 用**限制更紧的一边**决定格子大小，保证整板不超出窗口；多余的边居中留白。
- 若窗口过小致 `target < MIN_CELL`，`cellSize` 被夹到 `14`，棋盘会略超出窗口（可接受，正常尺寸不会）。

### 像素→格子坐标
```java
int c = (e.getX() - originX) / cellSize;   // 整数除法自动截断为格子索引
int r = (e.getY() - originY) / cellSize;
if (越界) return;
if (BUTTON1) onLeftClick(r,c); else onRightClick(r,c);
```
- 用 `mousePressed`（而非 `mouseClicked`）是因为**右键一按下就触发**，更灵敏，也避免选中/拖拽的歧义。

### onLeftClick / chord
```java
onLeftClick(r,c):
  if (revealed) chord(r,c);            // 数字格快速翻开
  res = model.open(r,c);
  if (res==REVEALED) SoundPlayer.playClick();  // 点到非雷 → 愉悦音
  repaint();
```
`chord(r,c)`（快速翻开，经典玩法）：
```java
n = aroundCount(r,c); if (n==0) return;
cnt = 数(r,c) 周围被标记的邻居数;
if (cnt != n) return;                      // 标记数不等于数字，不做
for 每个未标记且未翻开的邻居: 
    res = model.open(邻居);
    if (res==REVEALED) openedAny=true;
    if (res==LOST || res==WON) break;      // 输了/赢了立刻停
if (openedAny) SoundPlayer.playClick();
repaint();
```
- 例如数字 “3”，周围恰好标了 3 面旗 → 把剩下 5 个未标记邻居一次翻开，提速。

### onRightClick
```java
was = model.isFlagged(r,c);
model.toggleFlag(r,c);
now = model.isFlagged(r,c);
if (was != now) { if (now) playMark(); else playUnmark(); }  // 状态真的变了才响
repaint();
```

### 绘制（状态机 paintCell）
每个格子视觉状态：
| 条件 | 视觉 |
|---|---|
| 已翻开 + 是雷 | 浅色底+描边 + **炸弹图** |
| 已翻开 + 非雷 + 数字>0 | 浅色底+描边 + 着色数字 |
| 已翻开 + 非雷 + 数字0 | 浅色底+描边（空白） |
| 未翻开 + 已标记 | 灰浮雕底 + 红旗（若已结束且非雷 → 红× 标错） |
| 未翻开 + 未标记 | 灰浮雕底 |

- **paintCover 浮雕**：灰底 + 上/左 3px 亮边 + 下/右 3px 暗边，做出“凸起按钮”的立体感。
- **drawNumber 居中**：`FontMetrics.stringWidth` 算水平居中，`(cs - fm.getHeight())/2 + fm.getAscent()` 算垂直居中（修正了文字基线）。
- **drawIcon**：按格子边长 `cs` 缩放；**MINE** 直接 `drawImage(mineBomb)`（`cs` 缩放、居中）；FLAG=旗杆+红旗三角+底座；WRONG=红×。
- **为什么整板自绘**：Windows 系统外观会**忽略 `JButton.setBackground`**，若用按钮，未翻开/已翻开的颜色区分失效；自绘则完全可控，且能保证“每个格子是固定正方形”。

---

## 三、ui.AppWindow.java —— 关键点

### 构造 & 计时器
```java
super("扫雷游戏 — 三关扫雷");
setDefaultCloseOperation(EXIT_ON_CLOSE);
setIconImage(MinesweeperIcon.create(48)); // 任务栏/窗口图标
setJMenuBar(buildMenuBar());
buildStatusBar();
startLevel(1);          // 默认开第一关
pack(); setLocationRelativeTo(null); setMinimumSize(getSize());
```
- `Timer clock`（`javax.swing.Timer`，1 秒 tick）在**EDT**上运行；`onGameStart` 启动、`onGameOver` 停止。

### buildMenuBar —— 菜单结构
- `JRadioButtonMenuItem` + `ButtonGroup` 管“难度”互斥（初级/中级/高级/自定义级别...）。
- 菜单项加 `GameIcons` 图标；`新游戏` 设置 `F2` 快捷键（`KeyStroke`）。
- “关于”用 **Java 文本块** `"""..."""` 呈现多行说明（比 `\n` 拼接清晰）。

### newGame(cfg)
```java
this.current = cfg;
clock.stop(); elapsed = 0; timeLabel.setText("用时：0 秒");
levelLabel/mineLabel 更新;
if (boardHolder != null) remove(boardHolder);       // 移除旧棋盘
MineFieldPanel field = new MineFieldPanel(cfg, listener);
// listener：onMineCountChanged→更新标签；onGameStart→计时；onGameOver→handleWin/handleLose
JPanel wrapper = new GradientPanel();                // 渐变边框容器
wrapper.add(field, BorderLayout.CENTER);             // 棋盘铺满
boardHolder = wrapper; add(wrapper, CENTER); revalidate(); repaint();
```
- 每局新建一个 `MineFieldPanel`（内含新 `MineField`），天然隔离。

### handleLose / handleWin —— 过关流程
- `levelIndex`：`-1`=普通/自定义；`1..3`=第几关。
- `handleWin` 三分支：未过完→问“继续下一关？”；=3→“通关成功重开？”；其它→“再来一局？”；都用 `GameIcons` 图标。

### openCustomDialog
```java
prev = selectedDifficulty();                 // 记住当前难度
CustomLevelDialog dlg = new CustomLevelDialog(this); dlg.setVisible(true);
cfg = dlg.getConfig();
if (cfg != null) { levelIndex=-1; newGame(cfg); }
else if (prev != null) prev.setSelected(true);   // 取消 → 恢复原选中
```

### GradientPanel（内部类）
`BorderLayout` + 8px 空边框，`paintComponent` 画纵向渐变（`GradientPaint`），作为棋盘外围的柔和背景。

---

## 四、ui.CustomLevelDialog.java

- 构造 `super(owner,"自定义级别",true)`：**模态**（必须先处理）。
- 3 个 `SpinnerNumberModel`（行 5~30、列 5~50、雷 1~449）+ 名称框。
- **雷数上限联动**：`ChangeListener` 里 `max = 行×列-1`，`mineModel.setMaximum(max)` 并夹紧当前值（防止用户把雷数设到 ≥ 总格数）。
- `onOk` 校验：名称非空、≤12 字符、`雷数 < 行×列`，不满足弹 `ERROR` 框且不关闭；通过则 `config = new GameConfig(...)` 并 `dispose()`。
- `getConfig()`：确定返回配置，取消返回 `null`。

---

## 五、ui.MineSweeper.java
```java
SwingUtilities.invokeLater(() -> {          // 所有 UI 建在 EDT
    UIManager.setLookAndFeel(system...);    // 系统外观，更原生
    new AppWindow().setVisible(true);
});
```
- Swing 非线程安全：必须把窗口创建放进 EDT。

---

## 六、audio.SoundPlayer.java —— 波形合成细节
- **Note record**：`(double freq, int ms)`。
- **play(Note...)**：起**守护线程**（`t.setDaemon(true)`），开 `SourceDataLine`，逐音符 `writeTone`，音间 `Thread.sleep(25)`，最后 `drain/stop/close`；`muted` 则直接 return；无声卡等异常静默忽略。
- **writeTone(l, freq, ms)**：
  ```
  total = 44100 * ms / 1000          // 采样点总数
  buf = new byte[total*2]            // 16 位单声道
  for i in [0,total):
    t   = i / 44100                  // 当前时刻（秒）
    pos = i / total                  // 归一化位置
    env = pos<0.03 ? pos/0.03 : (pos>0.97 ? (1-pos)/0.03 : 1)   // 3% 淡入淡出
    v   = (sin(2π f t) + 0.35·sin(2π·2f·t)) * 0.5 * env        // 基波+二次谐波
    s   = (short)( v * Short.MAX_VALUE * 0.3 )                 // 音量 0.3
    buf[2i]   = s & 0xff            // 小端
    buf[2i+1] = (s>>8) & 0xff
  line.write(buf)
  ```
  - **谐波**：加 `0.35·sin(2f)` 让音色不单薄、更柔和。
  - **淡入淡出**：避免音符突然开始/结束的“爆音”。
  - **音量**：`VOLUME=0.3`（原 0.8 的约 1/3）。
  - **幅度安全**：`|v|max ≈ (1+0.35)*0.5 = 0.675`，乘 `Short.MAX*0.3 ≈ 6634`，远不出界。

---

## 七、gfx（图标）
- **MinesweeperIcon.create(s)**：圆角方块 + 蓝渐变 + 顶部柔光/底部暗角 + 引线(`QuadCurve2D`) + 火花 + 弹体 + 高光/反光；按边长方缩。用于窗口图标 & 生成 icon.ico。
- **GameIcons.bomb/flag/clock/check(size)**：纯 `Graphics2D` 画的 `ImageIcon`，用于菜单/状态栏/对话框，风格统一、不依赖外部图片。

---

## 八、packaging.MakeIcon.java —— ICO 二进制格式
```
ICONDIR(6B): reserved(2)=0 | type(2)=1 | count(2)=7
ICONDIRENTRY ×7(各16B): width(1,256→0) | height(1) | colorCount(1)=0 | reserved(1)=0
                        | planes(2)=1 | bitCount(2)=32 | bytesInRes(4) | imageOffset(4)
之后按 offset 依次拼接 7 张 PNG
```
- **小端序**：用 `writeShortLE/writeIntLE` 手动写字节（`DataOutputStream` 是大端，会出错）。

---

## 九、packaging.SfxProgram.cs —— 版本标记自解压器
```
baseDir = %TEMP%\MineSweeperGame
exePath = baseDir\MineSweeper\MineSweeper.exe
marker  = baseDir\app.zip.len

zipLen = 内嵌资源 "minesweeper.app.zip" 的字节长度
needExtract = !File.Exists(exePath) || !File.Exists(marker) || marker内容 != zipLen
若 needExtract：
  删除并重建 baseDir → 把资源 zip 写入 baseDir\app.zip
  → ZipFile.ExtractToDirectory(zipPath, baseDir) → 删 zip → 写 marker = zipLen
启动 exePath（工作目录=解压目录），WaitForExit；异常用 MessageBox 提示
```
- **为什么用“zip 长度”做版本标记**：一旦重新打包（内容变化），zip 长度随之变化 → 标记不符 → 强制重新解压，避免运行旧缓存；而内容没变时标记一致 → 秒开。既保证“每次都是最新”，又保留快速启动。

---

## 十、跨领域要点
- **线程**：Swing 组件与 `Timer` 全在 EDT；音效在独立守护线程 → 不会有线程安全问题，也不阻塞界面。
- **模型/视图分离**：`core` 无 Swing 依赖可单测；`ui` 通过事件回调驱动，二者解耦。
- **打包**：模块裁剪（体积 ~148→70MB）、`-Xms32m -Xmx256m`（内存 ~430→220MB 私有）、`--add-modules` 最小化 JRE。
