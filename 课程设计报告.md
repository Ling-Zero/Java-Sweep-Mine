# 《Java程序设计》课程设计报告

| 项目 | 内容 |
|---|---|
| 课程设计 | 《Java程序设计》课程设计 |
| 题目 | 基于 Java Swing 的"三关扫雷"游戏 |
| 学　　院 | 信息技术工程学院 |
| 班　　级 | （请填写） |
| 姓　　名 | （请填写） |
| 学　　号 | （请填写） |
| 成　　绩 | （请填写） |
| 指导老师 | 王伟　教授 |
| 日期 | 2026 年 09 月 04 日 |

---

## 一、项目简介

**功能描述**：本课题实现了一款基于 Java Swing 的桌面扫雷游戏——"三关扫雷"。游戏采用"模型/视图分离"的分层架构，包含：经典扫雷玩法（左键翻开、右键标记、数字与空白区域自动展开、快速翻开 chord）；自定义级别（可自定行数、列数、雷数与级别名称）；过关玩法（第一关→第二关→第三关，每关难度递增，过关后询问是否继续）；游戏音效（点击/标记/过关/失败四种提示音，全部用正弦波实时合成，无需音频文件）；自适应棋盘（随窗口大小缩放并居中）；以及自绘图标（地雷/红旗/时钟/对勾、应用图标，全部由 Graphics2D 绘制）。另提供体积、内存优化后的免安装版与单文件版（.exe）打包。

**有无参考他人代码**：无。核心算法（布雷、BFS 空白扩散、胜负判定、快速翻开）、界面自绘、音效合成、图标绘制、打包脚本等均自行实现，未直接复用他人代码。

**参考 git 地址或博客地址**：本地 Git 仓库 `CurriculumDesign`（课程项目源码与文档均已归档，提交记录含功能基线 `v1.0-baseline` 与多轮功能/文档提交）。

**个人负责任务**：独立完成全部模块——核心逻辑模型、棋盘视图与自绘、主窗口与菜单、三关过关流程、自定义级别对话框、音效合成、图标绘制、界面美化、打包与体积/内存优化。

**团队博客链接**：无（个人项目）。

**个人博客链接**：无（如需得 B 以上可补充）。

---

## 二、功能架构图

系统整体功能架构如下（本项目为个人独立完成，图中全部功能均为本人负责）：

```
                扫雷游戏（三关扫雷）
        ┌──────────────────────────────────────────────┐
        │  主窗口 AppWindow（JFrame）                   │
        │   菜单栏  游戏/难度/关卡/音效/帮助           │
        │   状态栏  当前级别 | 剩余雷数 | 用时          │
        │   中央区  棋盘（GradientPanel + 自适应）      │
        └───────────────┬──────────────┬───────────────┘
                        │              │
     ┌──────────────────▼───┐     ┌────▼───────────────┐
     │ 棋盘视图 MineFieldPanel│     │ 设置 CustomLevelDialog│
     │ 整板自绘 · 鼠标 · 音效 │     │ 行/列/雷/名称 校验    │
     └──────────┬───────────┘     └────────────────────┘
                │ 读状态 / open() / toggleFlag()
     ┌──────────▼───────────┐
     │ 模型 MineField（核心） │
     │ 布雷 · BFS 扩散 · 判定 │
     └──┬───────┬───────┬───┘
        │       │       │
   ┌────▼──┐ ┌─▼─────┐ ┌▼────────┐
   │GameConfig│ │LevelManager│ │Listener事件│
   └────────┘ └───────┘ └─────────┘
   （辅助） SoundPlayer(音效) · MinesweeperIcon/GameIcons(图标)

 打包：javac/jar → jpackage(免安装版) → zip → csc(单文件版 .exe)
```

> 说明：架构图按"模型/视图分离"分层；`core`（模型）不依赖界面，`ui`（界面）依赖 `core/audio/gfx`。图中棋盘、模型、音效、图标、打包等均属本人负责部分。

---

## 三、个人任务简述

### 1. 完成的任务与功能

| 序号 | 完成功能与任务 | 描述 |
|---|---|---|
| 1 | 核心逻辑模型 | 使用面向对象与"模型/视图分离"思想设计 `core.MineField`：二维数组存储雷/翻开/标记/数字，实现**首击安全布雷**、**BFS 空白扩散**、胜利/失败判定，与界面完全解耦，可独立测试 |
| 2 | 棋盘视图与自绘 | 用 `Graphics2D` **整板自绘**（规避 Windows 外观忽略按钮背景色问题），实现固定正方形格子、未翻开/已翻开颜色分明、数字配色、地雷/红旗/标错图标，以及**随窗口自适应缩放并居中** |
| 3 | 主窗口与菜单 | `AppWindow`（JFrame）实现菜单栏（游戏/难度/关卡/音效/帮助）、状态栏（级别/剩余雷数/用时）、三关过关流程与自定义级别入口 |
| 4 | 快速翻开 chord | 数字格周围已标记雷数等于该数字时，一次翻开其余未标记邻居（经典扫雷提速玩法） |
| 5 | 音效合成 | `SoundPlayer` 用 **正弦波+二次谐波+淡入淡出** 实时合成 PCM，实现点击/标记/过关/失败音；播放置于守护线程，不阻塞界面，无需任何音频文件 |
| 6 | 图标与美化 | `MinesweeperIcon`（应用/窗口图标）、`GameIcons`（地雷/红旗/时钟/对勾）全部用 `Graphics2D` 绘制；状态栏/菜单/对话框加图标、棋盘外围渐变背景 |
| 7 | 打包与优化 | `jpackage` 生成自包含免安装版（`--add-modules` 裁剪模块，体积约 148MB→70MB）；`--java-options -Xms32m -Xmx256m` 限制内存（私有内存约 430MB→220MB）；用 `.NET csc` 自解压器生成单文件版 .exe（带版本标记防旧缓存） |

### 2. Git 提交记录截图

（请在能打开的 Git 客户端中截取本仓库 `git log --oneline --decorate` 的提交历史，并粘贴在此处，最多 1 页。提交记录示意如下：）

```
34e79e5 更新代码详解：对齐当前代码…
6eff69e 简化代码注释：移除冗余/过详注释…
b1f4ce1 (tag: v1.0-baseline) v1.0 功能基线：扫雷游戏（三关扫雷）完整可玩版本
```

---

## 四、本人负责功能详解

> 说明：以下均为本人实现的模块；带 ⭐ 为重难点/特色部分。每个模块给出"简述 + 关键代码"。在 Word 中可将关键代码块截图粘贴。

### 1. ⭐面向对象设计与分层结构（包图/类图）

**简述**：整个系统按职责分成四个包，依赖方向单一（`ui` 依赖 `core/audio/gfx`，`core` 不依赖任何界面层），实现"高内聚、低耦合"，且核心模型可独立单测。

**包图/类图（文字版）**：
```
minesweeper
├─ core    GameConfig(record)  LevelManager  MineField
├─ ui      MineSweeper(主类)  AppWindow  MineFieldPanel  CustomLevelDialog
├─ audio   SoundPlayer
└─ gfx     MinesweeperIcon  GameIcons
依赖：ui → core/audio/gfx
```

**关键代码**：核心模型只依赖 `ArrayDeque` 与 `Random`，不含任何 Swing 类，从而可无头测试。
```java
public class MineField {
    private final boolean[][] mines, revealed, flagged;
    private final int[][] around;
    private final Listener listener;   // onGameStart/onGameOver/onMineCountChanged
    public OpenResult open(int r, int c) { ... }
}
```

### 2. ⭐首击安全布雷（拒绝采样 + 数字统计）

**简述**：第一次点击后才布雷，且保证被点的那格不是雷；随后统计每个格子周围 8 格的地雷数作为"数字"。难点是避免首击格/重复布雷，以及相邻雷的正确计数。

**关键代码**：
```java
private void placeMines(int safeR, int safeC) {
    int placed = 0;
    while (placed < mineCount) {
        int r = rnd.nextInt(rows), c = rnd.nextInt(cols);
        if ((r == safeR && c == safeC) || mines[r][c]) continue; // 跳过首击格/已布雷格
        mines[r][c] = true; placed++;
    }
    for (每颗雷) for (8 邻格) if (inRange) around[邻][]++;   // 相邻雷会被各自 +1，数字正确
}
```

### 3. ⭐BFS 空白扩散（open 决策树 + floodReveal）

**简述**：点击空白格会自动翻开整片空白及其一圈数字格。用 `ArrayDeque<int[]>` 做 BFS，`around==0` 才继续入队扩散，`revealed` 守卫保证幂等不重复计数；用显式队列避免大棋盘递归爆栈。

**关键代码**：
```java
public OpenResult open(int r, int c) {
    if (!started) { started = true; placeMines(r, c); listener.onGameStart(); }  // 首击安全
    if (mines[r][c]) { finish(false); return LOST; }
    floodReveal(r, c);
    if (openedSafe == rows * cols - mineCount) { finish(true); return WON; }
    return REVEALED;
}

private void floodReveal(int r, int c) {
    ArrayDeque<int[]> q = new ArrayDeque<>(); q.add(new int[]{r, c});
    while (!q.isEmpty()) {
        int[] p = q.poll();
        if (!inRange(p)||revealed||flagged||mines) continue;
        revealed[pr][pc]=true; openedSafe++;
        if (around[pr][pc]==0) for (8 邻) q.add(邻);
    }
}
```

### 4. ⭐整板自绘 + 自适应布局（MineFieldPanel）

**简述**：不使用 `JButton`（Windows 系统外观会忽略按钮背景色，导致未翻开/已翻开无法区分），改为继承 `JPanel` 重写 `paintComponent` **整板自绘**，保证每个格子为固定正方形、未翻开（灰浮雕）与已翻开（浅色带描边）颜色分明；并**随窗口大小自适应**：`cellSize = clamp(min(宽/列, 高/行), 14, 48)` 并居中。鼠标点击用同样的格子坐标换算。

**关键代码**：
```java
int target = (int) Math.min((double)availW/cols, (double)availH/rows);
cellSize = Math.max(MIN_CELL, Math.min(MAX_CELL, target));
originX = (getWidth()-boardW)/2; originY = (getHeight()-boardH)/2;
// 鼠标：int c = (e.getX()-originX)/cellSize;
```

### 5. ⭐音效合成（SoundPlayer）

**简述**：无需任何音频文件，用数学公式生成 16 位单声道 PCM 并写入声卡。难点是让音色柔和、无爆音、不阻塞界面。做法：基波叠加二次谐波使音色更饱满；首尾 3% 线性淡入淡出防爆音；播放放到守护线程。

**关键代码**：
```java
double env = (pos < fade) ? pos/fade : (pos > 1-fade ? (1-pos)/fade : 1.0);
double v = (Math.sin(2*PI*freq*t) + 0.35*Math.sin(2*PI*2*freq*t)) * 0.5 * env;
short s = (short)(v * Short.MAX_VALUE * VOLUME);   // VOLUME=0.3
buf[i*2] = (byte)(s & 0xff); buf[i*2+1] = (byte)((s>>8) & 0xff);  // 小端
```

### 6. 自定义级别（CustomLevelDialog）

**简述**：模态对话框，输入行数(5~30)、列数(5~50)、雷数(1~449)与级别名称；行列变化时雷数上限联动为"行×列-1"；确定时校验名称非空且 ≤12 字符、雷数<行×列。

### 7. 打包与体积/内存优化

**简述**：`jpackage --add-modules java.base,java.desktop,java.logging,jdk.unsupported` 只打包所需模块（体积约 148MB→70MB）；`--java-options "-Xms32m -Xmx256m"` 限制内存（私有内存约 430MB→220MB）；用 `.NET csc` 写自解压器把游戏 zip 内嵌为单文件 `.exe`，并以"内嵌 zip 长度"作版本标记（内容变化则强制重解压，避免旧缓存）。

---

## 五、课程设计感想

本次课设我独立完成了一款"三关扫雷"游戏。过程中最困扰我的是几个"坑"：

1. **Windows 外观忽略 `JButton` 背景色**：最初用按钮做格子，发现未翻开/已翻开全是白色、无法分辨。后来改为继承 `JPanel` 整板自绘，既解决了颜色问题，又保证了格子是固定正方形、能随窗口缩放。这让我体会到"界面绘制"不能只靠现成控件，也要理解底层渲染。
2. **音效"有爆音"与"太大"**：直接播放正弦波会有咔咔声且偏响。后来加入首尾淡入淡出、二次谐波、并把音量系数降到 0.3 才自然。原来"好听"也是可以算出来的。
3. **单文件打包**：起初用 Windows 自带的 IExpress，发现它按"文件数量上限"限制（约 100 个），而 jpackage 精简运行时仍有上百个文件；最终改用 `.NET csc` 自解压器内嵌 zip，并用"zip 长度当版本号"避免用户跑到旧缓存。
4. **体积与内存**：默认 JVM 堆很大，打包默认含大量模块。通过 `--add-modules` 裁剪和 `-Xms/-Xmx` 限堆，让占用明显下降。

这些过程让我明白了"做出能跑的"和"做得干净、可靠、好用"之间的差距，也更理解了面向对象分层、事件回调、以及"模型与视图分离"的价值。

---

## 六、展望

1. **不足之处**：目前无存档/最佳用时记录，音色较单一，联网/多人对战未实现；代码未引入单元测试；图标与界面仍有进一步美化空间。改进方向：加入记录与排行榜、用音频文件或更丰富的合成参数改善音色、引入 JUnit 对核心模型做自动化测试、考虑用 GraalVM 原生镜像进一步降低内存占用。
2. **后续想做的事**：进一步学习 Java 17+ 新特性（record、文本块、switch 表达式、`List.of` 等）并把项目重构得更纯粹；学习 `javax.sound.sampled` 更多用法；研究 `jpackage`/`jlink` 更精细的裁剪；为后续课程/项目积累"桌面应用 + 打包发布"的完整经验。
