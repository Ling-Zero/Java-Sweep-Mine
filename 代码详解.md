# 扫雷游戏（三关扫雷）—— 代码详解

> 当前版本：分层分包结构，含自适应棋盘、统一炸弹图标、版本标记单文件打包。

## 一、总体架构（按职责分层）

```
minesweeper/
├─ core/   GameConfig(record) · LevelManager · MineField   —— 纯逻辑/配置，无界面依赖
├─ ui/     MineSweeper(主类) · AppWindow · MineFieldPanel · CustomLevelDialog
├─ audio/  SoundPlayer                                      —— 音效合成
└─ gfx/    MinesweeperIcon · GameIcons                      —— 图标绘制
packaging/ MakeIcon.java(生成 icon.ico) · SfxProgram.cs(单文件自解压)
```

依赖方向单向：`ui` → `core` / `audio` / `gfx`；`core` 可独立测试。

---

## 二、core 包（核心逻辑/配置）

### 1. GameConfig.java —— 配置 record
```java
public record GameConfig(int rows, int cols, int mines, String name) { }
```
- Java 16+ `record`：不可变值对象，自动生成构造器、`rows()/cols()/mines()/name()` 访问器、equals/hashCode/toString。
- 四要素：行数、列数、雷数、级别名称。配置创建后不再变化。

### 2. LevelManager.java —— 关卡与难度配置
- `TOTAL_LEVELS = 3`。
- `LEVELS`：三关配置（8×8/10 → 12×12/20 → 16×16/40），难度逐关提升。
- `PRESETS`：普通难度（初级 9×9/10、中级 16×16/40、高级 16×30/99）。
- `getLevel(i)` / `getTotalLevels()` 供菜单和过关流程使用。
- “关卡”（通关玩法）与“难度”（直接选局）两套配置互不影响。

### 3. MineField.java —— 核心逻辑模型（重点）
完全不含 Swing，只用 `ArrayDeque` 和 `Random`。

**状态字段**：`mines/revealed/flagged`(boolean[][]）、`around`(int[][]，即数字)、`started/finished`、`openedSafe/flagCount`。

**事件接口 Listener**：`onGameStart()`、`onGameOver(boolean won)`、`onMineCountChanged(int remaining)` —— 模型通过回调通知界面，不直接操作 UI。

**open(r,c)（左键翻开）**：
```java
if (finished)          return IGNORED;          // 已结束
if (revealed[r][c])    return ALREADY_OPEN;     // 交给视图做 chord
if (flagged[r][c])     return FLAGGED;
if (!started) { started=true; placeMines(r,c); onGameStart(); } // 首次点击才布雷
if (mines[r][c]) { finish(false); return LOST; }
floodReveal(r,c);
if (openedSafe == rows*cols - mineCount) { finish(true); return WON; }
return REVEALED;
```
- 用 `OpenResult` 枚举描述结果，视图据此决定播音效/处理快速翻开 —— 职责清晰。
- 先布雷再判断踩雷 → 第一击必定安全。

**placeMines（布雷）**：随机布雷直到放够 `mineCount`，跳过首击格与已布雷格；再对每颗雷的 8 邻格 `around++` 算出数字。

**floodReveal（空白扩散，BFS）**：
```java
队列加入 (r,c)；
while (队非空) {
  弹出 (pr,pc)；if (越界||已翻开||已标记||是雷) continue;
  revealed=true; openedSafe++;
  if (around==0) 把 8 个邻居入队；   // 数字格只翻自身不再扩散
}
```
即“点空白自动开一片”。用 `int[]{r,c}` 存坐标节省对象分配。

**toggleFlag**：切换标记、更新 `flagCount`、通知剩余雷数；已翻开/已结束忽略。

**finish(won)**：获胜 → 给剩余地雷自动插旗并把剩余雷数清零；失败 → 把所有地雷设置为已翻开。最后 `onGameOver(won)`。

---

## 三、ui 包（界面）

### 4. MineSweeper.java —— 入口
```java
SwingUtilities.invokeLater(() -> { ... });   // 放到 EDT
UIManager.setLookAndFeel(system ...);         // 系统外观
new AppWindow().setVisible(true);
```
- Swing 非线程安全，UI 必须在 EDT 上；用系统外观更原生化。

### 5. AppWindow.java —— 主窗口
继承 `JFrame`，负责菜单栏、状态栏、棋盘、关卡流程。

- **构造**：设标题/窗口图标(`MinesweeperIcon.create(48)`)、菜单栏、状态栏、`startLevel(1)` 默认开第一关、`pack()`、居中、设最小尺寸。
- **buildMenuBar**：
  - `游戏(G)`：新游戏(F2)、`难度`(初级/中级/高级/自定义级别... 用 `JRadioButtonMenuItem`+`ButtonGroup` 互斥)、`关卡（三关扫雷）`(三项)、`音效`(checkbox)、`退出`；`帮助(H)`→`关于`。
  - 各菜单项用 `GameIcons` 加图标；“自定义级别...”点击 → `openCustomDialog()`。
- **buildStatusBar**：级别名加粗、剩余雷数带地雷图标、用时带时钟图标；浅灰底 + 顶部细线；右侧灰色斜体操作提示。
- **startLevel(idx)**：`levelIndex = idx; newGame(第 idx 关配置);`。
- **newGame(cfg)**：
  - 重置计时、更新“当前级别/剩余雷数”标签；
  - 建一个新的 `MineFieldPanel(cfg, listener)`，listener 把 `onMineCountChanged`/`onGameStart`/`onGameOver` 转发给 AppWindow（更新标签、启停计时、触发胜负流程）；
  - 面板放入 `GradientPanel`（渐变边框容器）并铺满。
- **handleLose**：`playLose()` + “是否重新开始？”（带炸弹图标）。
- **handleWin**：`playWin()`，按三种情况询问：
  - 关卡未过完 → “是否继续下一关？”是则 `startLevel(next)`；
  - 通过第三关 → “通关成功，是否重新开始？”是则 `startLevel(1)`；
  - 普通/自定义 → “是否再来一局？”是则 `newGame(current)`。
- **openCustomDialog**：记住之前选中的难度；弹模态对话框；确定 → `newGame(cfg)`；取消 → 恢复原选中。
- **GradientPanel**：内部类，`BorderLayout` + 8px 空边框，绘制纵向渐变作为棋盘外围背景。

### 6. MineFieldPanel.java —— 棋盘视图（重点）
持有 `MineField`，整板自绘、处理鼠标、播音效，并把模型事件转发出去。

- **自适应窗口**：不固定格子大小；`paintComponent` 里按可用空间算
  `cellSize = clamp( min(可用宽/列数, 可用高/行数), MIN_CELL, MAX_CELL )`，并居中（记录 `originX/originY`）。
- **像素→格子**：`(e.getX()-originX)/cellSize`，用 `mousePressed`（右键一按下就触发）。
- **onLeftClick**：已翻开 → `chord()`；否则 `model.open()`，返回 `REVEALED` 播愉悦音；`repaint()`。
- **onRightClick**：`model.toggleFlag()`，状态真的变了才播 标记音/取消音。
- **chord（快速翻开）**：数字格周围已标记雷数==该数字时，翻开其余未标记邻居；按 `open` 返回值判断输赢并及时停止。
- **paintCell**：已翻开=浅色+描边（雷→炸弹图标 / 数字→着色数字）；已标记=浮雕底+红旗（结束且非雷→红×标错）；未翻开=灰底浮雕。
- **drawNumber**：字体大小随格子 `cs*0.5` 变化，`FontMetrics` 居中。
- **drawIcon**：按格子边长 `cs` 缩放画 地雷/红旗/红×：
  - **MINE**：直接 `drawImage(mineBomb)`（`GameIcons.bomb` 的图，含短引线+火花，与应用图标一致），尺寸 `cs-8` 居中。
  - FLAG：旗杆+红旗三角+底座；WRONG：红×。
- 说明：整板自绘而非 JButton，是为了避免 Windows 外观忽略按钮背景色，从而保证“固定正方形格子 + 未翻开/已翻开颜色分明”。

### 7. CustomLevelDialog.java —— 自定义级别对话框（模态）
- `super(owner, "自定义级别", true)`：模态，用户必须先处理它。
- 三个 `JSpinner`（行数 5~30、列数 5~50、雷数 1~449）+ 名称 `JTextField`。
- **雷数上限联动**：行列变化时把雷数上限设为 `行×列-1` 并夹紧。
- **onOk 校验**：名称非空且 ≤12 字符、`雷数 < 行×列`，失败弹错误框不关闭；通过生成 `GameConfig` 并关闭。
- `getConfig()`：确定返回配置，取消返回 `null`。

---

## 四、audio / gfx

### 8. SoundPlayer.java —— 音效合成（无外部文件）
- `Note(freq, ms)` record 描述音符；各音效是一串 Note（点击 C5-E5-G5 琶音、标记 A5-D6、过关上行、失败下行）。
- `play(Note...)`：**守护线程** 上打开 `SourceDataLine`，逐音符 `writeTone`，音间 `Thread.sleep(25)`，最后 drain/stop/close —— 不阻塞界面；无声卡异常静默忽略。
- `writeTone`：生成 16 位单声道 PCM。核心
  `v = (sin(2πft) + 0.35·sin(2π·2f·t)) * 0.5 * env`，首尾 **3% 淡入淡出** 防爆音，乘 `VOLUME=0.3` 控制音量（原 0.8 的约 1/3）。

### 9. MinesweeperIcon.java —— 应用/窗口图标
- `create(int s)`：绘制“蓝渐变圆角背景 + 炸弹(引线/火花/弹体/高光)”，按边长等比缩放。
- 运行时 `AppWindow.setIconImage(create(48))` 作为窗口/任务栏图标；打包时 `MakeIcon` 用它生成 icon.ico。

### 10. GameIcons.java —— 界面小图标
- `bomb/flag/clock/check(int size)` 四个图标（`ImageIcon`），全部 Graphics2D 绘制，用于菜单、状态栏、对话框，风格统一，不依赖外部图片。

---

## 五、packaging（打包工具）

### 11. MakeIcon.java —— 生成 icon.ico
- 对每个尺寸 `sizes={16,24,32,48,64,128,256}` 调用 `MinesweeperIcon.create(s)` → 编成 PNG → 按 **ICO 格式** 排列：
  - 6 字节头 `reserved/type(1)/count`；
  - 每个 16 字节条目 `width`(256记0)/`height`/`colorCount`/`reserved`/`planes(1)`/`bitCount(32)`/`bytesInRes`/`offset`；
  - 最后拼接各尺寸 PNG。
- 关键：**ICO 是小端序**，用 `writeShortLE/writeIntLE` 手动写字节（`DataOutputStream` 是大端序会出错）。

### 12. SfxProgram.cs —— 单文件自解压启动器（.NET csc 编译）
- 被编译成 `扫雷游戏.exe`，把 `dist\MineSweeper.zip` 内嵌为资源 `minesweeper.app.zip`。
- **版本标记**：运行时读取内嵌 zip 的字节长度，与临时目录标记文件 `app.zip.len` 比较；
  - 若 exe 不存在或标记不符 → 强制重新解压（避免运行到旧缓存），再写标记。
  - 标记一致 → 跳过解压（“秒开”）。
- 以解压目录为工作目录启动 `MineSweeper.exe` 并 `WaitForExit`；出错用 `MessageBox` 提示。
- 编译命令：`csc /target:winexe /win32icon:... /resource:zip,minesweeper.app.zip ...`。

---

## 六、关键算法

| 算法 | 位置 | 思路 |
|---|---|---|
| 布雷（首击安全） | `MineField.placeMines` | 随机放雷，跳过一次点击格与已放格；再给每雷的 8 邻计数出数字 |
| 空白扩散 | `MineField.floodReveal` | BFS 队列，around==0 则扩展，数字格只翻自身 |
| 打开 | `MineField.open` | 守卫状态 → 首击布雷 → 踩雷判 LOST / 否则翻开+判胜 |
| 标记 | `MineField.toggleFlag` | 切换 + 更新计数 + 通知 |
| 快速翻开 | `MineFieldPanel.chord` | 数字格周围标记数==数字 → 翻开其余邻居 |
| 胜/负判定 | `MineField.finish` | 翻开安全格数==总格-雷数 → 通关，否则踩雷 |
| 音效合成 | `SoundPlayer.writeTone` | 正弦+谐波，淡入淡出包络，音量系数 |
| 自适应格子 | `MineFieldPanel.paintComponent` | 按可用空间算格子边长并居中 |
| 图标 | `MinesweeperIcon`/`GameIcons` | Graphics2D 矢量绘制，任意尺寸 |
| ICO | `MakeIcon` | PNG 内嵌 + 小端序 ICO 头/目录 |
| 单文件 | `SfxProgram.cs` | 内嵌 zip → 版本标记判断 → 解压 → 启动 |

---

## 七、运行流程（一次游戏时序）

1. `main` 在 EDT 创建 `AppWindow` → 构建菜单/状态栏 → `startLevel(1)` 开第一关。
2. 鼠标 `mousePressed` → 换算 (r,c) → `onLeftClick/onRightClick`。
3. 左键 → `model.open`：首击布雷+启动计时；踩雷→`finish(false)`→`onGameOver(false)`→`handleLose`；安全→`floodReveal`，全开→`finish(true)`→`onGameOver(true)`→`handleWin`。
4. 右键 → `toggleFlag` → 刷新剩余雷数 + 音效。
5. 每次操作 `repaint()`，`paintComponent` 读最新模型状态画画面。

---

## 八、线程模型
- **EDT**：窗口、菜单、面板、对话框、`javax.swing.Timer` 均在事件分发线程，无线程安全问题。
- **音效**：`SoundPlayer` 每个 `play` 起一个守护线程合成播放，不阻塞界面。

---

## 九、打包与体积/内存优化
```bash
javac -encoding UTF-8 -d out $(Get-ChildItem src -Recurse -Filter *.java | % FullName)
jar --create --file dist-jar/minesweeper.jar --main-class minesweeper.ui.MineSweeper -C out .
jpackage --type app-image --icon dist/icon.ico --input dist-jar --name MineSweeper \
         --main-jar minesweeper.jar --main-class minesweeper.ui.MineSweeper --dest dist \
         --add-modules java.base,java.desktop,java.logging,jdk.unsupported \
         --java-options "-Xms32m -Xmx256m"
Compress-Archive -Path dist\MineSweeper -DestinationPath dist\MineSweeper.zip
csc ... /out:扫雷游戏.exe /win32icon:... /resource:dist\MineSweeper.zip,minesweeper.app.zip ...
```
- 模块裁剪：运行时 ~148MB → ~70MB。
- 内存：`-Xms32m -Xmx256m` 限制堆，私有内存 ~430MB → ~220MB。
- 单文件版：内嵌 zip + 版本标记自解压器。
